The file `test.exe` is obtained by compiling `test.hs` using command

```txt
ghc -O -outputdir "./build" -o "./test.exe" test.hs
```

`ghc` is the [Glasgow Haskell Compiler](https://www.haskell.org/ghc/).